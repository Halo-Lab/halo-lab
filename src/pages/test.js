// This is a test page and it should not be exist on release. But, please, do not remove it now.

import React from 'react';

import Providers from '@components/Providers';

const Test = () => {
  return (
    <Providers>
      <div>Test</div>
    </Providers>
  );
};

export default Test;
